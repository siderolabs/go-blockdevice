# commit to be tagged for the new release
commit = "HEAD"

project_name = "go-blockdevice"
github_repo = "talos-systems/go-blockdevice"
match_deps = "^github.com/(talos-systems/[a-zA-Z0-9-]+)$"

# previous = -
# pre_release = true

# [notes]
